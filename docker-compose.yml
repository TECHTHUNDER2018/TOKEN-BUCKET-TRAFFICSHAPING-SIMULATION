version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "8080:8080"
      - "9000:9000"
    networks:
      - sim-network



networks:
  sim-network:
    driver: bridge
